services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=pedal_pal_dev'
      - 'POSTGRES_PASSWORD=postgres'
      - 'POSTGRES_USER=postgres'
    ports:
      - '5432:5432'
